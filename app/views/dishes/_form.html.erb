<%= simple_form_for [@culinarystyle, @dish] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.association :culinarystyle, as: :radio_buttons %>
  <%= f.submit "add a dish", class: "btn btn-primary" %>
<% end %>
